# The versions this repo's numbers were produced with. Direct dependencies
# only; pip resolves the rest. Same pins as pyproject.toml - this file exists
# for people who want the scripts without installing the package.
#
#   pip install -r requirements.txt
#
# Collector.
websockets==17.0.1
httpx==0.28.1

# Research tier. Nothing in the collector imports these.
numpy==2.5.2
torch==2.13.0
matplotlib==3.11.1

# Tests and lint.
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.2
aiohttp==3.14.3

# The measurements above ran against the CPU wheel, torch 2.13.0+cpu, from
#   https://download.pytorch.org/whl/cpu
# The PyPI wheel of the same version is what this file pins; use the CPU index
# if you do not want the CUDA payload.
